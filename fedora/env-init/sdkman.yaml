---
- name: Set sdkman environment variable
  hosts: localhost
  gather_facts: true

  tasks:
    - name: Set sdkman environment variable
      ansible.builtin.blockinfile:
        append_newline: true
        prepend_newline: true
        state: present
        path: "{{ ansible_env.HOME }}/DevOps/bashrc.d/env.bashrc"
        marker: "# {mark} SDKman"
        block: |
          export SDKMAN_DIR="$HOME/DevOps/sdk/sdkman"
